<UserControl x:Class="LCSC.App.Views.BotUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:appBehaviors="using:LCSC.App.Behaviors"
             xmlns:controls="using:CommunityToolkit.WinUI.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:helpers="using:LCSC.Core.Helpers"
             xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
             xmlns:lctWorks="using:LCTWorks.WinUI.Controls"
             xmlns:local="using:LCSC.App.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="using:CommunityToolkit.WinUI"
             mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="500"
                              MinWidth="340"
                              MaxWidth="500" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid Margin="8"
              Background="{StaticResource AppColor1}"
              CornerRadius="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="68" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid Padding="16">
                <StackPanel HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Orientation="Horizontal"
                            Spacing="8">
                    <FontIcon Glyph="&#xE756;" />
                    <TextBlock FontSize="16"
                               Text="BOT LOG" />
                </StackPanel>

                <StackPanel HorizontalAlignment="Right"
                            Orientation="Horizontal"
                            Spacing="4">
                    <Button Command="{x:Bind ViewModel.ConnectBotCommand, Mode=OneWay}"
                            Style="{ThemeResource AccentButtonStyle}"
                            Visibility="{x:Bind ViewModel.IsDisconnected, Mode=OneWay}">
                        <FontIcon Glyph="&#xE768;" />
                    </Button>
                    <Button Command="{x:Bind ViewModel.DisconnectBotCommand, Mode=OneWay}"
                            Visibility="{x:Bind ViewModel.IsConnected, Mode=OneWay}">
                        <FontIcon Glyph="&#xE71A;" />
                    </Button>
                </StackPanel>
            </Grid>
            <Grid Grid.Row="1"
                  Padding="16"
                  Background="#101010">
                <ScrollView x:Name="TextScrollViewer">
                    <RichTextBlock HorizontalAlignment="Stretch"
                                   VerticalAlignment="Stretch"
                                   FontFamily="Cascadia Mono">
                        <interactivity:Interaction.Behaviors>
                            <appBehaviors:ConsoleMirrorBehavior />
                        </interactivity:Interaction.Behaviors>
                    </RichTextBlock>
                </ScrollView>
            </Grid>
        </Grid>
        <ScrollViewer Grid.Column="2"
                      Margin="0,8"
                      HorizontalAlignment="Stretch">
            <Grid RowSpacing="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <controls:SettingsExpander Description="Lista de configuraciones en uso por el bot"
                                           Header="Config"
                                           HeaderIcon="{ui:FontIcon Glyph=&#xE713;}"
                                           IsEnabled="{x:Bind ViewModel.IsConnected, Mode=OneWay}">
                    <StackPanel Orientation="Horizontal"
                                Spacing="8">
                        <ComboBox MinWidth="220"
                                  ItemsSource="{x:Bind ViewModel.Guilds, Mode=OneWay}"
                                  SelectedItem="{x:Bind ViewModel.SelectedGuild, Mode=TwoWay}" />
                        <Button Command="{x:Bind ViewModel.RefreshCommand}"
                                IsEnabled="{x:Bind ViewModel.IsRankingIdle, Mode=OneWay}">
                            <FontIcon Glyph="&#xE895;" />
                        </Button>
                    </StackPanel>
                </controls:SettingsExpander>
                <controls:SettingsExpander Grid.Row="1"
                                           Description="Lista de acciones relacionadas con el ranking"
                                           Header="Ranking"
                                           HeaderIcon="{ui:FontIcon Glyph=&#xE902;}"
                                           IsEnabled="{x:Bind ViewModel.IsConnected, Mode=OneWay}"
                                           IsExpanded="True">
                    <controls:SettingsExpander.Items>
                        <controls:SettingsCard HorizontalContentAlignment="Stretch"
                                               Description="Ejecuta 'Actualizar' y luego 'Mostrar' secuencialmente."
                                               Header="Actualizar y mostrar">
                            <StackPanel Orientation="Horizontal"
                                        Spacing="8">
                                <lctWorks:ThemedButton Command="{x:Bind ViewModel.UpdateAndDisplayRankCommand}"
                                                       Content="Actualizar y mostrar"
                                                       IsEnabled="{x:Bind ViewModel.IsRankingIdle, Mode=OneWay}"
                                                       Style="{StaticResource AccentThemedButtonStyle}">
                                    <lctWorks:ThemedButton.CommandParameter>
                                        <x:Boolean>False</x:Boolean>
                                    </lctWorks:ThemedButton.CommandParameter>
                                </lctWorks:ThemedButton>
                                <lctWorks:ThemedButton Command="{x:Bind ViewModel.CancelUpdateRankCommand}"
                                                       Content="Cancelar"
                                                       IsEnabled="{x:Bind ViewModel.IsRankingBusy, Mode=OneWay}"
                                                       Style="{StaticResource DangerThemedButtonStyle}" />
                            </StackPanel>
                        </controls:SettingsCard>
                        <controls:SettingsCard HorizontalContentAlignment="Stretch"
                                               Description="Estas acciones solo actualizan el ranking y almacenan los resultados."
                                               Header="Actualizar">
                            <StackPanel Orientation="Horizontal"
                                        Spacing="8">
                                <Button Command="{x:Bind ViewModel.UpdateRankCommand}"
                                        Content="Actualizar"
                                        IsEnabled="{x:Bind ViewModel.IsRankingIdle, Mode=OneWay}" />
                                <Button Command="{x:Bind ViewModel.UpdateRankForcedCommand}"
                                        Content="Actualizar (forzado)"
                                        IsEnabled="{x:Bind ViewModel.IsRankingIdle, Mode=OneWay}" />
                            </StackPanel>
                        </controls:SettingsCard>
                        <controls:SettingsCard HorizontalContentAlignment="Stretch"
                                               Description="Muestra el ranking, puede escoger si incluir o no a los miembros restringidos"
                                               Header="Mostrar">
                            <StackPanel Orientation="Horizontal"
                                        Spacing="8">
                                <Button Command="{x:Bind ViewModel.DisplayRankCommand}"
                                        Content="Mostrar"
                                        IsEnabled="{x:Bind ViewModel.IsRankingIdle, Mode=OneWay}">
                                    <Button.CommandParameter>
                                        <x:Boolean>False</x:Boolean>
                                    </Button.CommandParameter>
                                </Button>
                                <Button Command="{x:Bind ViewModel.DisplayRankCommand}"
                                        Content="Mostrar (Restringidos)"
                                        IsEnabled="{x:Bind ViewModel.IsRankingIdle, Mode=OneWay}">
                                    <Button.CommandParameter>
                                        <x:Boolean>True</x:Boolean>
                                    </Button.CommandParameter>
                                </Button>
                            </StackPanel>
                        </controls:SettingsCard>
                        <controls:SettingsCard HorizontalContentAlignment="Stretch"
                                               Description="Limpia las regiones de temporadas pasadas."
                                               Header="Limpiar">
                            <StackPanel Orientation="Horizontal"
                                        Spacing="8">
                                <lctWorks:ThemedButton Command="{x:Bind ViewModel.PruneRegionsCommand}"
                                                       Content="Limpiar regiones"
                                                       IsEnabled="{x:Bind ViewModel.IsRankingIdle, Mode=OneWay}"
                                                       Style="{ThemeResource WarningThemedButtonStyle}" />
                            </StackPanel>
                        </controls:SettingsCard>
                    </controls:SettingsExpander.Items>
                </controls:SettingsExpander>
                <controls:SettingsExpander Grid.Row="2"
                                           Description="Lista de opciones para enviar mensajes"
                                           Header="Mensajes"
                                           HeaderIcon="{ui:FontIcon Glyph=&#xEBDB;}"
                                           IsEnabled="False">
                    <controls:SettingsExpander.Items>
                        <controls:SettingsCard>
                            <controls:SettingsCard.Header>
                                <StackPanel Margin="0,16"
                                            Spacing="8">
                                    <StackPanel Orientation="Horizontal"
                                                Spacing="8">
                                        <Button Content="Test"
                                                Style="{ThemeResource AccentButtonStyle}" />
                                    </StackPanel>
                                </StackPanel>
                            </controls:SettingsCard.Header>
                        </controls:SettingsCard>
                    </controls:SettingsExpander.Items>
                </controls:SettingsExpander>
                <controls:SettingsExpander Grid.Row="3"
                                           Header="Debug"
                                           HeaderIcon="{ui:FontIcon Glyph=&#xEC7A;}"
                                           Visibility="{x:Bind helpers:BuildHelper.IsDebugBuild}">
                    <controls:SettingsExpander.Items>
                        <controls:SettingsCard>
                            <controls:SettingsCard.Header>
                                <StackPanel Margin="0,16"
                                            Spacing="8">
                                    <StackPanel Orientation="Horizontal"
                                                Spacing="8">
                                        <lctWorks:ThemedButton Content="Content"
                                                               Style="{ThemeResource AccentThemedButtonStyle}" />
                                        <lctWorks:ThemedButton Glyph="check_circle"
                                                               GlyphFontFamily="{StaticResource MaterialSymbolsFontFamily}"
                                                               Style="{ThemeResource AccentThemedButtonStyle}" />
                                        <lctWorks:ThemedButton Content="Check"
                                                               Glyph="check_circle"
                                                               GlyphFontFamily="{StaticResource MaterialSymbolsFontFamily}"
                                                               Style="{ThemeResource AccentThemedButtonStyle}" />
                                    </StackPanel>
                                </StackPanel>
                            </controls:SettingsCard.Header>
                        </controls:SettingsCard>
                    </controls:SettingsExpander.Items>
                </controls:SettingsExpander>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>