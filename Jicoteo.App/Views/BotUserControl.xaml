<UserControl x:Class="LSCC.App.Views.BotUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:appBehaviors="using:LSCC.App.Behaviors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:interactions="using:Microsoft.Xaml.Interactions.Core"
             xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
             xmlns:local="using:LSCC.App.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Padding="16"
          RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical"
                    Spacing="4">
            <Button Command="{x:Bind ViewModel.ConnectBotCommand, Mode=OneWay}"
                    Content="Iniciar" />
        </StackPanel>
        <Grid Grid.Row="1">
            <RichTextBlock HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"
                           FontFamily="Cascadia Mono">
                <interactivity:Interaction.Behaviors>
                    <appBehaviors:ConsoleMirrorBehavior />
                </interactivity:Interaction.Behaviors>
            </RichTextBlock>
        </Grid>
    </Grid>
</UserControl>